bun test --coverage --coverage-reporter=lcov --coverage-reporter=text --coverage-dir=/tmp/coverage
for d in runtime bootstrap; do if [ -d "adda-dev-runtime/src/$d" ] && ls adda-dev-runtime/src/$d/*.ts >/dev/null 2>&1; then bun build adda-dev-runtime/src/$d/*.ts --outdir /tmp/qg-build/$d/ --target bun; fi; done
tsc --noEmit
biome check adda-dev-runtime/src/
jq -e '.devDependencies["@types/bun"] == env.BUN_VERSION' package.json
